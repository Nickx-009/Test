/**
 * Provider registry exports.
 *
 * In the real implementation, you will construct provider instances with
 * env config (client ids, secrets, redirect URIs) and pass them into CalendarClient.
 */

export * as Google from './google';
export * as Microsoft from './microsoft';
