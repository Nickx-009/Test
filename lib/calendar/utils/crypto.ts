/**
 * Token encryption utilities (optional but recommended).
 *
 * This file will:
 * - Encrypt access/refresh tokens before storing in Supabase
 * - Decrypt tokens at runtime for provider API calls
 *
 * You can use libsodium, WebCrypto, or a KMS depending on your deployment.
 * NOTE: Empty stub by design.
 */
