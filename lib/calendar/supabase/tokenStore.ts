/**
 * Token store contract used by the calendar layer.
 *
 * This file exists so app code can depend on a stable interface.
 * The types themselves live in /lib/calendar/types/storage.
 */

export type { CalendarTokenStore, CalendarTokenKey, CalendarOAuthTokenRow } from '../types';
